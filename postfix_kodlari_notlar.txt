master_conf.c 
master dosyasını okuyan dosya.
max_proc dikkat buna göre listen açıyor yani aynı anda kabule edilebilecek smtp miktarı default 100

smtpd.c deki 
data_cmd fonksiyonunda data sonunda . gelen işareti sonunda veriyi proxy varsa
oraya gönderiyor.
biz burdada smtp_content_filter yapıp bunu dfilt pipe gonderemeye çalıaşcağız tüm alıcılar ile beraber.

smtp.c de data_cmd fonksiyonuna
    //HU--
    msg_info("HU--Recipient Ekliyoruz : %s", state->recipient);

    if (state->rcpt_count == 1)
        recipient_list_init(&state->rcpt_list, 1);
    recipient_list_add(&state->rcpt_list, state->rcpt_count,
            dsn_orcpt_addr ? dsn_orcpt_addr : "",
            dsn_notify,
            mystrdup(STR(state->addr_buf)),
            mystrdup(STR(state->addr_buf)));

    /*--HU-- --*/
    
    satırları eklendi bu sayede tüm kullanıcıları diziye kayıt edebiliyoruz alabiliyoruz.
    
qmgr_transport.c    qmgr_transport_select--satır hangi pipe gidecek. çalışma sırası 1
		    qmgr_transport_alloc-- bu fonksiyon ile mail_connect ile bağlantı yapılıyor.
qmgr_message.c	    qmgr_message_alloc--fonksiyonu ile messagla alıcı listesi sırası yapuılyıro.burda herşey normal.(alıcı listesi tam.)
qmgr.c              qmgr_loop--bu fonksiyon qmgr_active_drain çalıştııroy oda qmgr_transport_select çalışıtıyor.

